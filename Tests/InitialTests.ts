import { expect } from "chai";
import { viem } from "hardhat"

describe("Initial Tests", async () => {
  describe("Total Supply", async () => {
    it("Define total supply of FAST token", async () => {
      throw new Error("Not implemented");
    })
    it("Define fractional bps", async () => {
      throw new Error("Not implemented");
    });
    it("Minter can mint", async () => {
      throw new Error("Not implemented");
    });
    it("Mint allowance goes down after minting", async () => {
      throw new Error("Not implemented");
    });
  })
});